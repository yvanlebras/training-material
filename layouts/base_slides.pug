doctype html

html

//- if page.tutorial_name
  each tuto in tutorials
    if tuto.name == page.tutorial_name
      - var title = tuto.title

head

    meta( charset="utf-8" )
    title
      block title
        | placeholder
    link( rel="stylesheet" href= "/css/slides.css" )
    link( rel="stylesheet" href= "/css/font-awesome.css" id="theme" )

body

    textarea( id="source" )
      | name: inverse
      | layout: true
      | class: center, middle, inverse

      div( class="my-footer")
          span
            if logo == "GTN"
                img( src= site.url + site.logo style="height: 40px;")
            else
                img( src= logo style="height: 40px;" )
      |
      | ---
      |      
      |
      | ##  Welcome !
      |
      
      | This material is the result of a collaborative work. 
      br
      | Thanks to 
      a( href = site.url + "/" + name + "#contributors" ) all the contributors 
      | and the 
      a( href = "https://wiki.galaxyproject.org/Teach/GTN" ) Galaxy Training Network 
      | !
      br
      br
        
      if logo == "GTN"
          img( src= site.url + site.logo style="height: 100px;" )
      else
          img( src=  logo style="height: 40px;" )
          
      |
      | ---
      |
      
      
      h1 
        block title
          | placeholder

      |
      | ---
      |
      
      | ## Requirements

      | Before diving into this slide deck, we recommend you to have a look at:
      |
      //-each requirement in parent_topic.requirements
        
            if requirement.type == "internal"
              | - 
              a( href = site.url + requirement.link  ) #{ requirement.title }
            else if material.type == "external"
              | - 
              a( href = requirement.link ) #{ requirement.title }

      //-if page.tutorial_name
        each requirement in tutorial.requirements
          if requirement.type == "internal"
              | - 
              a( href = site.url + requirement.link ) #{ requirement.title }
          else if material.type == "external"
              | - 
              a( href = requirement.link ) #{ requirement.title }

      |
      | ---
      |
      
      block content
        #{ contents }
              
      |
      | .footnote[Found a typo? Something is wrong in this tutorial?
      br
      | Edit it on 
      //-a( href = site.github_repository + "/tree/master/" + topic_name + "/slides/" + topic_name ) GitHub
      | ]

    script( src="https://remarkjs.com/downloads/remark-latest.min.js" type="text/javascript")
    script( type="text/javascript" ).
      var slideshow = remark.create({navigation: {scroll: false,}});
      var hljs = remark.highlighter.engine;
